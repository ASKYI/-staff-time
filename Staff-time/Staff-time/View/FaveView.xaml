<UserControl x:Class="Staff_time.View.FaveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Staff_time.View"
             
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding FaveTaskNodes}" SelectedItem="{Binding SelectedTask}" AutoGenerateColumns="False" IsSynchronizedWithCurrentItem="True">

            <DataGrid.InputBindings>
                <MouseBinding
                 MouseAction="LeftDoubleClick"
                 Command="{Binding SelectTaskCommand}"
                 CommandParameter="{Binding FaveTaskNodes/}"/>
            </DataGrid.InputBindings>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Избранное:"  Binding="{Binding Path}" Width="*" IsReadOnly="True" ></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
